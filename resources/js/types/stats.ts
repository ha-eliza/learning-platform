export interface StudentStatsProps {
  average_grade: number;
  completed_count: number;
  pending_count: number;
}

export interface TeacherStatsProps {
  pending_verification_count: number;
  total_materials_published: number; 
}
